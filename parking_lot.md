# Parking lot — ai-automation-cost-estimator

Ideas that surfaced during the v1 build. NOT in v1 scope.

- **Cache-aware agent-loop mode** - model a multi-turn agent where the system prompt and tool defs are reused across turns with cache reads (the un-served slice from the discarded generic calculator). Cross-links to brief 01.
- **More platforms** - Workato, Pipedream, Gumloop, Lindy, Relevance AI.
- **Live provider pricing** - pull current model token prices instead of hardcoded defaults (needs a source feed; keep the manual override as the trustworthy fallback).
- **Downloadable spreadsheet** - export the model so teams can fork the assumptions.
- **Short writeup** - "the hidden cost of AI in no-code automations," a content piece for the automation audience.

Product-creep tripwire (doctrine T11): accounts, saved scenarios, or a pro tier means it has become an app. Stop and park.
